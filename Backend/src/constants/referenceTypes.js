const REFERENCE_TYPE = {
  ENQUIRY: 'enquiry',
  LEAD: 'lead',
  QUALIFICATION: 'qualification',
};

const REFERENCE_TYPE_VALUES = Object.values(REFERENCE_TYPE);

module.exports = { REFERENCE_TYPE, REFERENCE_TYPE_VALUES };
