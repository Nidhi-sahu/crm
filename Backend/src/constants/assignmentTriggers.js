const ASSIGNMENT_TRIGGER = {
  MANUAL: 'manual',
  REASSIGN: 'reassign',
  AUTO: 'auto',
  UNASSIGN: 'unassign',
};

const ASSIGNMENT_TRIGGER_VALUES = Object.values(ASSIGNMENT_TRIGGER);

module.exports = { ASSIGNMENT_TRIGGER, ASSIGNMENT_TRIGGER_VALUES };
